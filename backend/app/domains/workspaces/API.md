# Workspaces API

`GET/POST /api/v1/workspaces`, `GET/PUT/DELETE /api/v1/workspaces/{id}`, `POST /api/v1/workspaces/{id}/move-to-next-step`을 제공한다. 조회는 `workspaces:read`, 변경은 `workspaces:write`이며 사용자 소유 범위를 적용한다.
