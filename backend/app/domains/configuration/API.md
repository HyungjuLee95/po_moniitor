# Configuration API

`GET /configuration/bootstrap`, `GET/PUT /configuration/monitoring-policy`, `GET /configuration/rtims-check`, `GET /configuration/sap-po-check`를 제공한다. 공개 응답에는 내부 URL과 credential을 포함하지 않는다.
