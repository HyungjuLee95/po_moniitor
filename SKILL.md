# AI 작업 SKILL

## 시작

1. 루트 `README.md`, `MANUAL.md`, `ERROR.md`를 읽는다.
2. 요청을 frontend/backend와 도메인으로 분류한다.
3. 대상 영역과 도메인의 네 문서를 읽는다.
4. 기존 public API, 환경 변수, DB migration을 확인한다.

## 작업 원칙

- 관련 없는 도메인을 함께 수정하지 않는다.
- UI 권한 숨김만 믿지 않고 백엔드 권한을 검증한다.
- SAP URL과 인증정보를 프론트 응답에 포함하지 않는다.
- LLM에는 마스킹된 장애 문맥만 전달한다.
- 추정으로 API를 만들지 말고 계약을 문서에 먼저 기록한다.

## 완료

- 코드, 테스트, 문서가 같은 책임 경계를 따른다.
- 새 오류와 중요한 판단을 `ERROR.md` 또는 `MANUAL.md`에 반영한다.
- 실행·검증 결과를 남기고 미검증 항목을 명시한다.
