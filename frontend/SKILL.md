# Frontend SKILL

1. 요청 화면의 도메인 문서를 읽는다.
2. API 응답 타입과 로딩·오류·빈 상태를 정의한다.
3. UI는 해당 도메인에 구현하고 `src/app`에서 조립한다.
4. 권한별 노출과 모바일 레이아웃을 확인한다.
5. `package.json`의 실행 명령이 공식 `next` CLI를 사용하는지, Vinext/Vite/Worker 런타임이 다시 추가되지 않았는지 확인한다.
6. `npm run lint`, `npm test`를 통과시키고 문서를 갱신한다.
